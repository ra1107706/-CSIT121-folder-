package com.boostmytool;

public class String_Methods { public static void main (String[] args
)
{ String First ="Choas";

    //boolean result = First.equalsIgnoreCase("choas"); "checks if it spells correctly"
    //boolean result = First.isEmpty(); " This method sees if the main string is empty"
    //String result = First.toLowerCase(); "This method changes the output to lower cases"
    //String result = First.toUpperCase(); " This method changes the output too all upper cases"
    //char result = First.charAt(4); "This method returnes back the letter that is in numerical coding order"
    //String result = First.trim(); "Thi method trims back spaces that you moved in your main string"
    //String result = First.replace("s","z");"This method changes a single letter in your main string word to another that you choose"
 //int result = First.indexOf("h");" This method return a number value when you type in the letter that is in numercal order"
//int result = First.length();"This method counts the total of your main string method"
    System.out.println();
}
}
